<h1>Your Pal Requests</h1>

<h2>Requests You Sent (As Requester)</h2>
<table class="table">
  <thead>
    <tr>
      <th>Pal Email</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @pal_requests_as_mentee.each do |r| %>
      <tr>
        <td><%= r.mentor.email %></td>
        <td><%= r.status %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2>Requests You Received (As Requested Pal)</h2>
<table class="table">
  <thead>
    <tr>
      <th>Requester Email</th>
      <th>Status</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @pal_requests_as_mentor.each do |r| %>
      <tr>
        <td><%= r.mentee.email %></td>
        <td><%= r.status %></td>
        <td>
          <% if r.status == 'pending' %>
            <%= button_to 'Accept', pal_request_path(r, status: 'accepted'), method: :patch, class: "btn btn-success" %>
            <%= button_to 'Decline', pal_request_path(r, status: 'declined'), method: :patch, class: "btn btn-danger" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

